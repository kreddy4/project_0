#6 inputs
#4 outputs
#10 gates
INPUT(A0)
INPUT(A1)
INPUT(A2)
INPUT(B0)
INPUT(B1)
INPUT(B2)
OUTPUT(S0)
OUTPUT(S1)
OUTPUT(S2)
OUTPUT(Cout)
S0=XOR(A0,B0)
c = AND(A0,B0)
d = XOR(A1,B1)
S1=XOR(c,d)
e = AND(A1,B1)
j = AND(c,e)
f = XOR(A2,B2)
g = AND(A2,B2)
S2=XOR(j, f)
Cout=AND(j,g)
